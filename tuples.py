#tuples
#tuples are immudable, can't be changed or modified.  Lists can be changed.
coordinates = (4, 5) #xy coordinates
print(coordinates[0]) #index position inside brackets, which starts at 0 not 1.
#can create a list of tuples:
coordinates = [(4, 5), (6, 7), (80, 34)]
